{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "carrot-extension: compile",
      "type": "shell",
      "command": "npm.cmd",
      "args": ["run", "compile"],
      "options": {
        "cwd": "${workspaceFolder}/vscode-extension"
      },
      "problemMatcher": "$tsc",
      "group": "build"
    }
  ]
}
